<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  
    <!-- Title -->
    <title>{{ site.title }}</title>
  
    <!-- Meta -->
    <meta name="description" content="{{ page.summary }}" />
    <meta name="author" content="{{ page.author }}" />
  
    <!-- Styles & Scripts -->
    <link rel="preload stylesheet" as="style" href="{{ css.app.permalink }}" />
  
    <!-- Preload -->
    <link rel="preload" as="image" href="{{ abs-url "theme.png" }}" />
  
    <!-- Favicon -->
    <link rel="icon" href="{{ abs-url "favicon.ico" }}" />
    <link rel="apple-touch-icon" href="{{ abs-url "apple-touch-icon.png" }}" />
  
    <!-- Generator -->
  
    <!-- RSS -->
  
  </head>
  