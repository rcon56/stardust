<article class="post-single">

  <header class="post-title">
    <p>
      <time>{{ post.date }}</time>
      <span>{{ post.author }}</span>
      {{ end }}
    </p>
    <h1>{{ post.title }}</h1>
  </header>
  <section class="post-content">{{{ post.content }}}</section>

  <!-- Post Tags -->
   <footer class="post-tags">
   {{ #each post.tags as |t| }}
       <a href="{{ abs-url "tag" t }}">{{ t }}</a>
   {{ /each }}
   </footer>

  <!-- Post Nav -->
  

  <!-- Disqus -->
  
</article>

